{
  "name": "cropintel",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev:web": "pnpm --filter cropintel-frontend dev",
    "build:web": "pnpm --filter cropintel-frontend build",
    "dev:mobile": "pnpm --filter cropintel-mobile start",
    "build:shared": "pnpm --filter @cropintel/shared build"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
